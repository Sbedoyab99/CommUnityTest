
<EditForm EditContext="editContext" OnValidSubmit="OnValidSubmit">
    <DataAnnotationsValidator />
    <MudGrid Class="mb-4">
        <MudItem xs="12" sm="6">
            <MudTextField Label="Placa" @bind-Value="@Vehicle.Plate" />
            <ValidationMessage For="@(() => Vehicle.Plate)" />
        </MudItem>
        <MudItem xs="12" sm="6">
            <MudSelect @bind-Value="@Vehicle.Type" Label="Tipo" AnchorOrigin="Origin.BottomCenter">
                @foreach (var type in vehicleTypes)
                {
                    <MudSelectItem Value="@type" />
                }
            </MudSelect>
            <ValidationMessage For="@(() => Vehicle.Type)" />
        </MudItem>
        <MudItem xs="12" sm="6">
            <MudTextField Label="Descripción" @bind-Value="@Vehicle.Description" />
            <ValidationMessage For="@(() => Vehicle.Description)" />
        </MudItem>      
    </MudGrid>
    <MudItem>
        <MudButton Variant="Variant.Filled" Color="Color.Success" ButtonType="ButtonType.Submit"><i class="bi bi-floppy" /> Guardar Cambios</MudButton>
        <MudButton Variant="Variant.Filled" Color="Color.Primary" OnClick="ReturnAction"><i class="bi bi-arrow-left" /> Regresar</MudButton>
    </MudItem>
</EditForm>
    